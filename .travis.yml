sudo: true

language: php
php:
  - 7.1.11

cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.npm

addons:
  apt:
    packages:
      - nasm

before_install:
  - nvm use v8.9
  - composer install
  - npm install

script:
  - vendor/bin/phpcs --extensions='php' --ignore='bootstrap,vendor,node_modules,storage,public,database,_ide_helper.php' --standard='PSR2' .
  - vendor/bin/phpunit --colors
